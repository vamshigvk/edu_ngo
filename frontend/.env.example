# Base URL prepended to every axios request.
# Leave EMPTY in development so requests stay same-origin and the Vite dev
# proxy (see vite.config.js) forwards /api, /auth and /dashboard to :8000.
# In production set this to the backend origin, e.g. https://api.example.com
VITE_API_BASE=
